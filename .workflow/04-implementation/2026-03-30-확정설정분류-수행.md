# 수행 (Implementation - Do)

## 작업 정보
- 참조 태스크 문서: `.workflow/03-tasking/2026-03-30-확정설정분류-태스크확립.md`
- 활성 태스크: Task 2. 확정 설정 분류 폴더 생성

## 구현 메모
- 수정 대상:
  - `확정 설정/종족/`
  - `확정 설정/지역/`
  - `확정 설정/국가및단체/`
  - 각 폴더의 `목록.md`
- 판단 근거:
  - 기존 `확정 설정/개념/`과 동일한 깊이의 하위 폴더를 만든다.
  - 실제 확정 본문이 아니라 작업용 목록 문서를 둔다.
  - 국가 및 단체는 대륙 귀속과 범대륙 세력을 함께 적는다.
- 임시 검증:
  - 생성 후 `Get-ChildItem`과 `Get-Content`로 폴더/문서 존재 및 내용 확인 예정
